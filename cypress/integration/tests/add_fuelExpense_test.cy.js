import 'cypress-xpath';
import { signInWithStaticData, getStaticAccountData } from '../../support/utilities';


// describe('Fuel expenses tests', () => {
//   it('should check if at least one car is added', () => {
//     cy.visit('https://guest:welcome2qauto@qauto2.forstudy.space/');
//     cy.visit('https://guest:welcome2qauto@qauto2.forstudy.space/panel/expenses');
//     signInWithStaticData(staticAccountData);
//     cy.checkIfCarIsAdded().then(isAdded => {
//       if (isAdded) {
//         cy.log('At least one car is present.');
//         cy.xpath(`//button[@class='btn btn-primary'][text()='Add an expense']`).click();
//       }
//     });
//   });
// });
  